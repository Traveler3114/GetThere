<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:components="clr-namespace:GetThere.Components"
             x:Class="GetThere.Pages.RegistrationPage"
             Title="Create Account"
             BackgroundColor="#F8F9FA"
             Shell.NavBarIsVisible="False">

    <Grid>
        <components:AnimatedBackground />

        <!--  ── HERO AREA ──  -->
        <Border VerticalOptions="Start" HeightRequest="300" StrokeThickness="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Offset="0" Color="#4E85F2" />
                    <GradientStop Offset="1" Color="#512BD4" />
                </LinearGradientBrush>
            </Border.Background>

            <Grid RowDefinitions="Auto, *">
                <ImageButton Source="arrow_back_white.png" HeightRequest="30" WidthRequest="30" HorizontalOptions="Start" Margin="25,60,0,0" BackgroundColor="Transparent" Clicked="LoginButton_Clicked" />
                <VerticalStackLayout Grid.Row="1" Spacing="5" VerticalOptions="Center" Margin="0,0,0,30">
                    <Label Text="Join GetThere" FontSize="32" FontAttributes="Bold" TextColor="White" HorizontalOptions="Center" />
                    <Label Text="Create an account to start your journey" FontSize="14" TextColor="White" Opacity="0.8" HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Grid>
        </Border>

        <ScrollView Margin="24,250,24,10" VerticalOptions="Fill">
            <Border Margin="1" Padding="30,15,30,30" BackgroundColor="White" StrokeThickness="0" VerticalOptions="Start">
                <Border.Shadow>
                    <Shadow Brush="Black" Opacity="0.15" Offset="0,10" Radius="15" />
                </Border.Shadow>
                <Border.StrokeShape><RoundRectangle CornerRadius="16" /></Border.StrokeShape>

                <VerticalStackLayout Spacing="15">
                    <BoxView WidthRequest="40" HeightRequest="4" CornerRadius="2" BackgroundColor="#E0E0E0" HorizontalOptions="Center" Margin="0,0,0,10" />
                    
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Full Name" FontAttributes="Bold" FontSize="14" TextColor="Black" />
                        <Border Stroke="#EBEBEC" StrokeThickness="1" Padding="15,0" BackgroundColor="White" HeightRequest="45">
                            <Border.StrokeShape><RoundRectangle CornerRadius="8" /></Border.StrokeShape>
                            <Entry x:Name="FullNameEntry" Placeholder="Full Name" PlaceholderColor="#A0A0A0" TextColor="Black" />
                        </Border>
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="6">
                        <Label Text="Email" FontAttributes="Bold" FontSize="14" TextColor="Black" />
                        <Border Stroke="#EBEBEC" StrokeThickness="1" Padding="15,0" BackgroundColor="White" HeightRequest="45">
                            <Border.StrokeShape><RoundRectangle CornerRadius="8" /></Border.StrokeShape>
                            <Entry x:Name="EmailEntry" Placeholder="Email" PlaceholderColor="#A0A0A0" TextColor="Black" Keyboard="Email" />
                        </Border>
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="6">
                        <Label Text="Password" FontAttributes="Bold" FontSize="14" TextColor="Black" />
                        <Border Stroke="#EBEBEC" StrokeThickness="1" Padding="15,0" BackgroundColor="White" HeightRequest="45">
                            <Border.StrokeShape><RoundRectangle CornerRadius="8" /></Border.StrokeShape>
                            <Entry x:Name="PasswordEntry" Placeholder="Password" IsPassword="True" PlaceholderColor="#A0A0A0" TextColor="Black" />
                        </Border>
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="6">
                        <Label Text="Confirm Password" FontAttributes="Bold" FontSize="14" TextColor="Black" />
                        <Border Stroke="#EBEBEC" StrokeThickness="1" Padding="15,0" BackgroundColor="White" HeightRequest="45">
                            <Border.StrokeShape><RoundRectangle CornerRadius="8" /></Border.StrokeShape>
                            <Entry x:Name="ConfirmPasswordEntry" Placeholder="Confirm Password" IsPassword="True" PlaceholderColor="#A0A0A0" TextColor="Black" />
                        </Border>
                    </VerticalStackLayout>

                    <Label x:Name="ErrorLabel" TextColor="Red" FontSize="12" HorizontalOptions="Center" IsVisible="False" />

                    <Button x:Name="RegisterButton" Text="Create Account" Clicked="RegisterButton_Clicked" BackgroundColor="#009688" TextColor="White" CornerRadius="26" HeightRequest="52" FontAttributes="Bold" Margin="0,15,0,0" />

                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="5" Margin="0,10">
                        <Label Text="Already have an account?" TextColor="#A0A0A0" />
                        <Label Text="Sign In" TextColor="#009688" FontAttributes="Bold">
                            <Label.GestureRecognizers><TapGestureRecognizer Tapped="LoginButton_Tapped" /></Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>
                    <ActivityIndicator x:Name="BusyIndicator" IsRunning="False" IsVisible="False" Color="#512BD4" />
                </VerticalStackLayout>
            </Border>
        </ScrollView>
    </Grid>
</ContentPage>
